<%= form_with(model: concert) do |form| %>
  <% if concert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(concert.errors.count, "error") %> prohibited this concert from being saved:</h2>

      <ul>
        <% concert.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-label">
    <%= form.label :event, "Fecha del Concierto" %>
    <%= form.date_select :event , :start_year => 1980 %>
  </div>

  <div class="field">
    <%= form.label :tickets, "Asistentes" %>
    <%= form.number_field :tickets %>
  </div>

  <div class="field">
    <%= form.label :duration, "Duración en Minutos" %>
    <%= form.number_field :duration %>
  </div>


  <div class="field">
  <%= form.label :group, "Grupo" %>
    <select name="concert[group_id]">
    <% @groups.each do |group| %>
      <option value="<%= group.id %>"><%= group.name %></option> 
    <% end %>
    </select>
  </div>
  

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
